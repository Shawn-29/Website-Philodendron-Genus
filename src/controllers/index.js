const homeController = require('./home-controller');
const guideController = require('./growing-guide-controller');
const formController = require('./form-controller');
const plantListController = require('./plant-list-controller');
const articleController = require('./article-controller');
const notFoundController = require('./not-found-controller');

module.exports = {
    homeController,
    guideController,
    formController,
    plantListController,
    articleController,
    notFoundController
};